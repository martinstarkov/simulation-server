// This file is @generated by prost-build.
/// ===== Client -> Server =====
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct ClientMsg {
    /// Always include your client ID (first message SHOULD be Register).
    #[prost(string, tag = "1")]
    pub app_id: ::prost::alloc::string::String,
    #[prost(oneof = "client_msg::Body", tags = "2, 3, 4")]
    pub body: ::core::option::Option<client_msg::Body>,
}
/// Nested message and enum types in `ClientMsg`.
pub mod client_msg {
    #[derive(Clone, PartialEq, ::prost::Oneof)]
    pub enum Body {
        /// join/leave blocking cohort
        #[prost(message, tag = "2")]
        Register(super::Register),
        /// vote to advance current tick
        #[prost(message, tag = "3")]
        StepReady(super::StepReady),
        /// generic named request (optional data)
        #[prost(message, tag = "4")]
        Request(super::Request),
    }
}
#[derive(Clone, Copy, PartialEq, ::prost::Message)]
pub struct Register {
    /// true => blocking cohort member; false => viewer-only
    #[prost(bool, tag = "1")]
    pub contributes: bool,
}
#[derive(Clone, Copy, PartialEq, ::prost::Message)]
pub struct StepReady {
    /// "I'm ready to advance tick=t"
    #[prost(uint64, tag = "1")]
    pub tick: u64,
}
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct Request {
    /// optional correlation id (echoed back in Ack/Response)
    #[prost(uint64, tag = "1")]
    pub id: u64,
    /// e.g., "set-thrust"
    #[prost(string, tag = "2")]
    pub name: ::prost::alloc::string::String,
    /// optional opaque payload (interpret by 'name')
    #[prost(bytes = "vec", tag = "3")]
    pub data: ::prost::alloc::vec::Vec<u8>,
}
/// ===== Server -> Client =====
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct ServerMsg {
    #[prost(oneof = "server_msg::Body", tags = "1, 2, 3")]
    pub body: ::core::option::Option<server_msg::Body>,
}
/// Nested message and enum types in `ServerMsg`.
pub mod server_msg {
    #[derive(Clone, PartialEq, ::prost::Oneof)]
    pub enum Body {
        /// simulator publishes current tick (and optional data)
        #[prost(message, tag = "1")]
        State(super::State),
        /// acknowledgement to Request (no data)
        #[prost(message, tag = "2")]
        Ack(super::Ack),
        /// data-bearing reply to Request (optional)
        #[prost(message, tag = "3")]
        Response(super::Response),
    }
}
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct State {
    /// current tick
    #[prost(uint64, tag = "1")]
    pub tick: u64,
    /// optional snapshot/metadata (can be empty)
    #[prost(bytes = "vec", tag = "2")]
    pub data: ::prost::alloc::vec::Vec<u8>,
}
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct Ack {
    /// echoes Request.id (if any)
    #[prost(uint64, tag = "1")]
    pub id: u64,
    /// success/failure
    #[prost(bool, tag = "2")]
    pub ok: bool,
    /// optional message
    #[prost(string, tag = "3")]
    pub info: ::prost::alloc::string::String,
}
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct Response {
    /// echoes Request.id
    #[prost(uint64, tag = "1")]
    pub id: u64,
    /// echoes Request.name
    #[prost(string, tag = "2")]
    pub name: ::prost::alloc::string::String,
    /// payload for the reply
    #[prost(bytes = "vec", tag = "3")]
    pub data: ::prost::alloc::vec::Vec<u8>,
}
/// Generated client implementations.
pub mod simulator_api_client {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    use tonic::codegen::http::Uri;
    #[derive(Debug, Clone)]
    pub struct SimulatorApiClient<T> {
        inner: tonic::client::Grpc<T>,
    }
    impl SimulatorApiClient<tonic::transport::Channel> {
        /// Attempt to create a new client by connecting to a given endpoint.
        pub async fn connect<D>(dst: D) -> Result<Self, tonic::transport::Error>
        where
            D: TryInto<tonic::transport::Endpoint>,
            D::Error: Into<StdError>,
        {
            let conn = tonic::transport::Endpoint::new(dst)?.connect().await?;
            Ok(Self::new(conn))
        }
    }
    impl<T> SimulatorApiClient<T>
    where
        T: tonic::client::GrpcService<tonic::body::BoxBody>,
        T::Error: Into<StdError>,
        T::ResponseBody: Body<Data = Bytes> + std::marker::Send + 'static,
        <T::ResponseBody as Body>::Error: Into<StdError> + std::marker::Send,
    {
        pub fn new(inner: T) -> Self {
            let inner = tonic::client::Grpc::new(inner);
            Self { inner }
        }
        pub fn with_origin(inner: T, origin: Uri) -> Self {
            let inner = tonic::client::Grpc::with_origin(inner, origin);
            Self { inner }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> SimulatorApiClient<InterceptedService<T, F>>
        where
            F: tonic::service::Interceptor,
            T::ResponseBody: Default,
            T: tonic::codegen::Service<
                http::Request<tonic::body::BoxBody>,
                Response = http::Response<
                    <T as tonic::client::GrpcService<tonic::body::BoxBody>>::ResponseBody,
                >,
            >,
            <T as tonic::codegen::Service<
                http::Request<tonic::body::BoxBody>,
            >>::Error: Into<StdError> + std::marker::Send + std::marker::Sync,
        {
            SimulatorApiClient::new(InterceptedService::new(inner, interceptor))
        }
        /// Compress requests with the given encoding.
        ///
        /// This requires the server to support it otherwise it might respond with an
        /// error.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.send_compressed(encoding);
            self
        }
        /// Enable decompressing responses.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.accept_compressed(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_decoding_message_size(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_encoding_message_size(limit);
            self
        }
        /// One connection per client. Client sends Register/StepReady/Request;
        /// Server streams State/Ack/Response. Disconnect => auto-unregister.
        pub async fn link(
            &mut self,
            request: impl tonic::IntoStreamingRequest<Message = super::ClientMsg>,
        ) -> std::result::Result<
            tonic::Response<tonic::codec::Streaming<super::ServerMsg>>,
            tonic::Status,
        > {
            self.inner
                .ready()
                .await
                .map_err(|e| {
                    tonic::Status::unknown(
                        format!("Service was not ready: {}", e.into()),
                    )
                })?;
            let codec = tonic::codec::ProstCodec::default();
            let path = http::uri::PathAndQuery::from_static("/sim.SimulatorApi/Link");
            let mut req = request.into_streaming_request();
            req.extensions_mut().insert(GrpcMethod::new("sim.SimulatorApi", "Link"));
            self.inner.streaming(req, path, codec).await
        }
    }
}
/// Generated server implementations.
pub mod simulator_api_server {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    /// Generated trait containing gRPC methods that should be implemented for use with SimulatorApiServer.
    #[async_trait]
    pub trait SimulatorApi: std::marker::Send + std::marker::Sync + 'static {
        /// Server streaming response type for the Link method.
        type LinkStream: tonic::codegen::tokio_stream::Stream<
                Item = std::result::Result<super::ServerMsg, tonic::Status>,
            >
            + std::marker::Send
            + 'static;
        /// One connection per client. Client sends Register/StepReady/Request;
        /// Server streams State/Ack/Response. Disconnect => auto-unregister.
        async fn link(
            &self,
            request: tonic::Request<tonic::Streaming<super::ClientMsg>>,
        ) -> std::result::Result<tonic::Response<Self::LinkStream>, tonic::Status>;
    }
    #[derive(Debug)]
    pub struct SimulatorApiServer<T> {
        inner: Arc<T>,
        accept_compression_encodings: EnabledCompressionEncodings,
        send_compression_encodings: EnabledCompressionEncodings,
        max_decoding_message_size: Option<usize>,
        max_encoding_message_size: Option<usize>,
    }
    impl<T> SimulatorApiServer<T> {
        pub fn new(inner: T) -> Self {
            Self::from_arc(Arc::new(inner))
        }
        pub fn from_arc(inner: Arc<T>) -> Self {
            Self {
                inner,
                accept_compression_encodings: Default::default(),
                send_compression_encodings: Default::default(),
                max_decoding_message_size: None,
                max_encoding_message_size: None,
            }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> InterceptedService<Self, F>
        where
            F: tonic::service::Interceptor,
        {
            InterceptedService::new(Self::new(inner), interceptor)
        }
        /// Enable decompressing requests with the given encoding.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.accept_compression_encodings.enable(encoding);
            self
        }
        /// Compress responses with the given encoding, if the client supports it.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.send_compression_encodings.enable(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.max_decoding_message_size = Some(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.max_encoding_message_size = Some(limit);
            self
        }
    }
    impl<T, B> tonic::codegen::Service<http::Request<B>> for SimulatorApiServer<T>
    where
        T: SimulatorApi,
        B: Body + std::marker::Send + 'static,
        B::Error: Into<StdError> + std::marker::Send + 'static,
    {
        type Response = http::Response<tonic::body::BoxBody>;
        type Error = std::convert::Infallible;
        type Future = BoxFuture<Self::Response, Self::Error>;
        fn poll_ready(
            &mut self,
            _cx: &mut Context<'_>,
        ) -> Poll<std::result::Result<(), Self::Error>> {
            Poll::Ready(Ok(()))
        }
        fn call(&mut self, req: http::Request<B>) -> Self::Future {
            match req.uri().path() {
                "/sim.SimulatorApi/Link" => {
                    #[allow(non_camel_case_types)]
                    struct LinkSvc<T: SimulatorApi>(pub Arc<T>);
                    impl<
                        T: SimulatorApi,
                    > tonic::server::StreamingService<super::ClientMsg> for LinkSvc<T> {
                        type Response = super::ServerMsg;
                        type ResponseStream = T::LinkStream;
                        type Future = BoxFuture<
                            tonic::Response<Self::ResponseStream>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<tonic::Streaming<super::ClientMsg>>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as SimulatorApi>::link(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = LinkSvc(inner);
                        let codec = tonic::codec::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.streaming(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                _ => {
                    Box::pin(async move {
                        let mut response = http::Response::new(empty_body());
                        let headers = response.headers_mut();
                        headers
                            .insert(
                                tonic::Status::GRPC_STATUS,
                                (tonic::Code::Unimplemented as i32).into(),
                            );
                        headers
                            .insert(
                                http::header::CONTENT_TYPE,
                                tonic::metadata::GRPC_CONTENT_TYPE,
                            );
                        Ok(response)
                    })
                }
            }
        }
    }
    impl<T> Clone for SimulatorApiServer<T> {
        fn clone(&self) -> Self {
            let inner = self.inner.clone();
            Self {
                inner,
                accept_compression_encodings: self.accept_compression_encodings,
                send_compression_encodings: self.send_compression_encodings,
                max_decoding_message_size: self.max_decoding_message_size,
                max_encoding_message_size: self.max_encoding_message_size,
            }
        }
    }
    /// Generated gRPC service name
    pub const SERVICE_NAME: &str = "sim.SimulatorApi";
    impl<T> tonic::server::NamedService for SimulatorApiServer<T> {
        const NAME: &'static str = SERVICE_NAME;
    }
}
